#define DEBUG
#include <algorithm>
#include <iostream>
#include <string>
using namespace std;
const int maxcol = 60;
const int maxn = 105;
string filenames[maxn];
int main()
{
#ifdef DEBUG
    freopen("input55.txt", "r", stdin);
    freopen("output55.txt", "w", stdout);
#endif
    int n;
    while (cin>>n)
    {
        int M;
        for (int i = 0; i < n; i++)
        {
            cin >> op;
            if (op[0] == 'P')
            {
                s.push(ID)
            }
        }
    }

    return 0;
}