#include <algorithm>

void getSubSet(int digit,int start_length,int end_length,int *store_array)
//start_length is minimum digit of subset,end_length is maximum digit of subset(not including end_length)
{
    for(int i = 0; i < (1<<count); ++i){
        
    }
}