#include <iostream>
#include <vector>
using namespace std;
#define MAX_V 10 //the number of point
int main(int argc, char const *argv[])
{
    //Directed graph
    struct edge{
        int to,cost;
    };
    vector<edge> G[MAX_V];
    int V,E;
    for(int i=0;i<E;i++)
    {
        G[i].push_back(/*cost*/);
    
    }
    printf("hello");
    return 0;
    
}
